-- Basic colorscheme configuration
vim.cmd("colorscheme desert") -- Set a default colorscheme that's built-in

-- Don't try to load GitHub theme on startup to avoid errors
-- It will be configured later through the proper Packer setup
